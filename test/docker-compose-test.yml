services:
  bs-db-test:
    image: postgres:16-alpine
    container_name: bookstream-db-test
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB_TEST:-bookstream_test}
    ports:
      - "${DB_TEST_PORT:-5434}:5432"